=== 180-degree-awareness\AwarenessDrillClient.js ===
MATCH: if(chRef.x > 0 && chRef.x < cw && chRef.y > 0 && chRef.y < ch){          const chColor = pointerLocked ? '#00ff88' : '#ffbb00';                  ctx.fillStyle = chColor;         ctx.beginPath();         ctx.arc(chRef.x, chRef.y, 2, 0, Math.PI*2);         ctx.fill();          ctx.strokeStyle = chColo
========================================
=== angle-hold-trainer\AngleHoldClient.js ===
NOT FOUND
========================================
=== counter-strafe-trainer\CounterStrafeClient.js ===
NOT FOUND
========================================
=== deadzone-jiggle-snap\DeadzoneJiggleSnapClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         // Spread radius visualization         const spreadRadius = isDeadzone ? 0 : (currentSpeed / MAX_SPEED) * 35;          if (spreadRadius > 0) {           ctx.strokeStyle = 'rgba(239, 68, 68, 0.45)';           ctx.lineWidth =
========================================
=== evasive-slide-track\EvasiveSlideTrackClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         ctx.strokeStyle = isTracking ? "#00ff88" : "#fbbf24";         ctx.lineWidth = 1.2;         const gap = 3;         const len = 3;         ctx.beginPath();         ctx.moveTo(ch.x-gap-len, ch.y); ctx.lineTo(ch.x-gap, ch.y);  
========================================
=== flick-shot-training\ProFlickClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         const activeColor = pointerLocked ? crosshairColor : "#ffbb00";         ctx.strokeStyle = activeColor;                  // Outer Scope Ring         ctx.lineWidth = 2;         ctx.beginPath();         ctx.arc(ch.x, ch.y, cro
========================================
=== headshot-micro-adjust\HeadshotReflexClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         ctx.strokeStyle = pointerLocked ? "#00ff88" : "#fbbf24";         ctx.lineWidth = 1.2;                  const gap = 3;         const len = 3;         ctx.beginPath();         ctx.moveTo(ch.x-gap-len, ch.y); ctx.lineTo(ch.x-g
========================================
=== high-speed-kinetic-trainer\KineticTrainerClient.js ===
MATCH: if(crosshair.x > 0 && crosshair.x < cw && crosshair.y > 0 && crosshair.y < ch){         ctx.strokeStyle = pointerLocked ? '#00ff88' : '#ffbb00';         ctx.lineWidth = 2;         ctx.beginPath(); ctx.arc(crosshair.x, crosshair.y, 12, 0, Math.PI*2); ctx.stroke();         ctx.beginPath(); ctx.moveTo(
========================================
=== instant-response\InstantResponseClient.js ===
MATCH: if (chRef.x > 0 && chRef.x < cw && chRef.y > 0 && chRef.y < ch) {         const chColor = pointerLocked ? '#00ff88' : '#ffbb00';          ctx.fillStyle = chColor;         ctx.beginPath();         ctx.arc(chRef.x, chRef.y, 2, 0, Math.PI*2);         ctx.fill();          ctx.strokeStyle = chColor;     
========================================
=== micro-flick-burst\MicroFlickBurstClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         ctx.strokeStyle = pointerLocked ? "#00ff88" : "#ffaa00";         ctx.lineWidth = 1.2;                  const gap = 3;         const len = 3;         ctx.beginPath();         ctx.moveTo(ch.x-gap-len, ch.y); ctx.lineTo(ch.x-g
========================================
=== micro-flick-precision\MicroFlickPrecisionClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         ctx.strokeStyle = pointerLocked ? "#00ff88" : "#ffaa00";         ctx.lineWidth = 1.5;                  // Micro gaps         const gap = 4;         const len = 4;         ctx.beginPath();         ctx.moveTo(ch.x-gap-len, ch
========================================
=== parabolic-air-track\ParabolicAirTrackClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         const cc = pointerLocked ? "#00ff88" : "#ffaa00";         ctx.fillStyle = cc;         ctx.beginPath(); ctx.arc(ch.x, ch.y, 2, 0, Math.PI*2); ctx.fill();                  ctx.strokeStyle = cc;         ctx.lineWidth = 1.2;   
========================================
=== pixel-hold-swing\PixelHoldSwingClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         ctx.strokeStyle = "#00ff88";         ctx.lineWidth = 1.0;         const gap = 2;         const len = 2;         ctx.beginPath();         ctx.moveTo(ch.x-gap-len, ch.y); ctx.lineTo(ch.x-gap, ch.y);         ctx.moveTo(ch.x+ga
========================================
=== prefire-corner-clearer\PrefireCornerClearerClient.js ===
MATCH: if (chRef.x > 0 && chRef.x < cw && chRef.y > 0 && chRef.y < ch) {         const speed = Math.abs(playerVelocityX.current);         const moving = speed > 25;         ctx.strokeStyle = moving ? '#ef4444' : pointerLocked ? '#10b981' : '#f59e0b';         ctx.lineWidth = 2;                  // Error exp
========================================
=== pro-smooth-pursuit\ProSmoothPursuitClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         const cc = isHitRef.current ? '#00ff88' : '#ffaa00';         ctx.fillStyle = cc;         ctx.beginPath();         ctx.arc(ch.x, ch.y, 2, 0, Math.PI * 2);         ctx.fill();          ctx.strokeStyle = cc;         ctx.lineWi
========================================
=== pro-tracking\ProTrackingClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         const cc = pointerLocked ? '#00ff88' : '#ffaa00';         ctx.fillStyle = cc;         ctx.beginPath();         ctx.arc(ch.x, ch.y, 2, 0, Math.PI * 2);         ctx.fill();          ctx.strokeStyle = cc;         ctx.lineWidth
========================================
=== pubg-dmr-rhythm\PUBGDMRRhythmClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         // True bullet landing center (with recoil)         const rx = ch.x + recoilOffset.current.x;         const ry = ch.y - recoilOffset.current.y;           // Draw crosshair center (green)         ctx.strokeStyle = pointerLoc
========================================
=== pubg-drive-by\PUBGDriveByClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         ctx.strokeStyle = pointerLocked ? "#00ff88" : "#ffaa00";         ctx.lineWidth = 1.2;                  // Tracking circle reticle         ctx.beginPath();         ctx.arc(ch.x + recoilShakeX, ch.y + recoilShakeY, 12, 0, Mat
========================================
=== pubg-lead-drop\PUBGLeadDropClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         ctx.strokeStyle = pointerLocked ? "#00ff88" : "#ffaa00";         ctx.lineWidth = 1.2;                  // Scoped circle styling         ctx.beginPath();         ctx.arc(ch.x, ch.y, 22, 0, Math.PI*2);         ctx.stroke();  
========================================
=== reactive-sphere-tracking\ReactiveSphereTrackingClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         const cc = pointerLocked ? "#00ff88" : "#ffaa00";         ctx.fillStyle = cc;         ctx.beginPath(); ctx.arc(ch.x, ch.y, 2, 0, Math.PI*2); ctx.fill();                  ctx.strokeStyle = cc;         ctx.lineWidth = 1.5;   
========================================
=== recoil-control\RecoilControlClient.js ===
NOT FOUND
========================================
=== sound-spatial-reflex\SoundSpatialReflexClient.js ===
NOT FOUND
========================================
=== strafe-tracking\StrafeTrackingClient.js ===
NOT FOUND
========================================
=== target-acquisition\TargetAcquisitionClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         const activeColor = pointerLocked ? '#00ff88' : '#ffbb00';         ctx.strokeStyle = activeColor;         ctx.lineWidth = 2;         ctx.beginPath(); ctx.arc(ch.x, ch.y, 20, 0, Math.PI * 2); ctx.stroke();         ctx.lineWi
========================================
=== target-prioritization\TargetPrioritizationClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         const color = pointerLocked ? '#00ff88' : '#ffbb00';         ctx.strokeStyle = color;         ctx.lineWidth = 2;         ctx.beginPath();         ctx.moveTo(ch.x - 4, ch.y); ctx.lineTo(ch.x - 14, ch.y);         ctx.moveTo(c
========================================
=== vertical-air-pursuit\VerticalAirPursuitClient.js ===
MATCH: if (ch.x > 0 && ch.x < cvs.width && ch.y > 0 && ch.y < cvs.height) {         ctx.strokeStyle = isTracking ? "#00ff88" : "#fbbf24";         ctx.lineWidth = 1.2;         const gap = 3;         const len = 3;         ctx.beginPath();         ctx.moveTo(ch.x-gap-len, ch.y); ctx.lineTo(ch.x-gap, ch.y);  
========================================
=== vertical-air-track\VerticalAirTrackClient.js ===
MATCH: if (crosshair.x > 0 && crosshair.x < cw && crosshair.y > 0 && crosshair.y < ch) {         ctx.strokeStyle = pointerLocked ? '#00ff88' : '#ffbb00';         ctx.lineWidth = 2;         ctx.beginPath();         ctx.arc(crosshair.x, crosshair.y, 8, 0, Math.PI * 2);         ctx.stroke();          ctx.begi
========================================
